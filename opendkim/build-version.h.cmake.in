/* build-version.h.cmake.in - Generated by CMake.
**
** DKIMF_VERSION  -- the version string reported by the daemon.  On a tagged
**                   release build this is the plain project version; on an
**                   untagged development build it carries the git commit so
**                   the running daemon identifies exactly what it was built
**                   from (issue #350).
*/

#ifndef _BUILD_VERSION_H_
#define _BUILD_VERSION_H_

#define DKIMF_VERSION   "@DKIMF_VERSION@"

#endif /* _BUILD_VERSION_H_ */
